顺序表、链表、二叉树、哈希表、堆......=>数据结构
内存支持随机访问（闪现）
1.顺序表：所有的元素都在连续空间上存储（Java数组）；擅长访问。
2.链表(面试重要)：元素不必在连续的空间上存储；插入和删除更高效。
3.针对SeqList每个方法都单独写一段码进行测试，称为单元测试。
4.增加元素需要扩容，删除元素后不一定要缩容；则扩容情况可能会成为一种隐性的内存泄漏。
5.顺序表擅长随机访问，给定位置，能够有效获取/修改指定位置的值，时间复杂度是O(1)。
6.顺序表按值查找，插入、删除、时间复杂度是O(N)
7.对于尾插和尾删，时间复杂度是O(1)
链表：更擅长插入和删除--O(1)
1.链表对应的元素不是存储在连续的内存空间上，在每个元素上的额外用一个数据来保存说，下一个数据在那里。
2.节点：
3.java 中的一个.Java文件中可以存在多个class，但是只能有一个public的类,
4.每个节点都有next能找到下一个节点，知道第一个节点，就能找到剩下的链表的节点。
  很多面试题中：
5.面试常考：不带头的单向的非循环的链表;带头、带环、循环的工作常见
6.带头、双向、循环的链表
